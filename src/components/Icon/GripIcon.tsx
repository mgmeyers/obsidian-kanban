/**
 * ============================================================================
 * [실행 순서 #22] GripIcon.tsx — 드래그 핸들(grip) 아이콘 컴포넌트
 * ----------------------------------------------------------------------------
 * 단계: 실행-렌더링
 * 레인/아이템을 드래그할 때 사용자가 붙잡는 손잡이(grip) 모양을 나타내는 단순한
 * SVG 아이콘 컴포넌트이다. Icon.tsx와 달리 Obsidian의 setIcon API를 쓰지 않고
 * 직접 인라인 SVG 마크업으로 아이콘 모양을 정의한다. 전달받은 props(예: className,
 * onMouseDown 등 드래그 관련 이벤트 핸들러)를 그대로 <svg> 엘리먼트에 스프레드하여
 * 손잡이에 스타일이나 드래그 이벤트 리스너를 자유롭게 부착할 수 있도록 한다.
 * ============================================================================
 */
// props 타입을 any로 느슨하게 받아, 호출부에서 className/style/드래그 이벤트 핸들러 등을
// 자유롭게 전달할 수 있게 한 그립(grip) 아이콘 컴포넌트
export function GripIcon(props: any) {
  return (
    <svg {...props} xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16">
      {/* 2열 x 3행으로 배치된 6개의 작은 정사각형을 하나의 path로 그려 grip(손잡이) 모양을 표현 */}
      <path
        fill="currentColor"
        d="M5 3h2v2H5zm0 4h2v2H5zm0 4h2v2H5zm4-8h2v2H9zm0 4h2v2H9zm0 4h2v2H9z"
      ></path>
    </svg>
  );
}
